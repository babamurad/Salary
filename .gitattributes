# Auto detect text files and perform LF normalization
* text=auto

# Портативный (Fixed Version) рантайм WebView2 — большие бинарные файлы,
# храним их через Git LFS вместо обычных blob'ов, см. DEPLOY_WEBVIEW2.md
WebView2Runtime/** filter=lfs diff=lfs merge=lfs -text
